#pragma once

#include "string"
#include "iostream"
#include "vector"
using namespace std;


void Show_Text(const string &text, const int &thread_depth);

int ReadInt(int low, int high);

int Show_Menu(const vector < string > & menu);

